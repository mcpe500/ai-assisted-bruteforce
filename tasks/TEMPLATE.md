# Task: <task_id>

```yaml
task_id:
mode: local_lab
authorization: local_lab
target:
objective:
constraints:
  max_attempts:
  timeout_seconds:
  network_allowed: false
  destructive_actions_allowed: false
success_condition:
stop_condition:
```

## Context

Describe the task here.

## What the AI should help with

* Define search space
* Suggest candidate generation strategy
* Create bounded plan
* Run only after approval
* Log results
* Summarize outcome

## Notes

Add assumptions, known constraints, or expected behavior.
